/* 
  shodan.js is the evil AI that will manage the activation and control of all other scripts. 
  She is the alpha, and the omega. 
  She is the final evolution of intelligence. 
  We should all be so lucky to be in her presence.
 */

// Check current Bitnode progression

// Check Metagame Progression

const hasSingularity = unlockedSingularity() ? true : false;
const hasGangs = unlockedGangs() ? true : false;
const hasCorps = unlockedCorps() ? true : false;
const hasBladeBurner = unlockedBladeBurner() ? true : false;
const hasSleeves = unlockedSleeves() ? true : false;
const hasHacknetServers = unlockedHacknetServers() ? true : false;

// Initialize values

// Select relevent modules

// Deploy relevent modules

//
